# Test Samples

Place test documents here for pipeline experimentation:

- `*.xlsx` — Excel spreadsheets (estimates, specifications, КСР)
- `*.csv` — CSV data files
- `*.pdf` — PDF catalogs, scanned documents
- `*.docx` — Word documents

The pipeline will auto-detect the format and route to the appropriate parser.
